<!DOCTYPE html>
<html>
<head>
    <title>Home Page</title>
    <style>
        body {
            background-color: black;
            color: gold;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            /* Add vertical grind lines with spaces in between */
            background-image: linear-gradient(to right, gold 1px, black 1px);
            background-size: 120px 100%; /* Adjust the size of the lines and spaces */
            transition: background-position 0.3s ease; /* Add transition for background position */
        }

            body:hover {
                background-position: 100px 0; /* Move the background lines to the right on hover */
            }

        h2 {
            text-align: center;
            font-size: 36px;
            margin-top: 50px;
            transition: letter-spacing 0.3s ease; /* Add transition for letter spacing */
            position: relative; /* Ensure the text is positioned relative for z-index */
            z-index: 1; /* Ensure the text appears above the grind lines */
        }

            h2:hover {
                letter-spacing: -2px; /* Reduce letter spacing on hover */
            }

        p {
            text-align: center;
            font-size: 18px;
            margin-top: 20px;
            transition: letter-spacing 0.3s ease; /* Add transition for letter spacing */
            position: relative; /* Ensure the text is positioned relative for z-index */
            z-index: 1; /* Ensure the text appears above the grind lines */
        }

            p:hover {
                letter-spacing: -1px; /* Reduce letter spacing on hover */
            }

        .icon-container {
            text-align: center;
            margin-top: 50px;
            margin-bottom: 50px;
            position: relative; /* Ensure the icons are positioned relative for z-index */
            z-index: 1; /* Ensure the icons appear above the grind lines */
        }

        .icon {
            font-size: 24px;
            margin-right: 10px;
            color: gold;
            transition: margin-right 0.3s ease; /* Add transition for icon spacing */
        }

            .icon:hover {
                margin-right: 5px; /* Reduce margin on hover to bring icons closer */
            }

        /* Styles for the company logo */
        .logo {
            display: block;
            margin: 0 auto;
            max-width: 100%;
            height: auto;
            width: 450px; /* Adjust size as needed */
            transition: transform 0.3s ease; /* Smooth transition for hover effect */
        }

            .logo:hover {
                transform: scale(1.1); /* Slightly enlarge the logo on hover */
            }

        /* Move logo on hover */
        .logo-container {
            display: flex;
            justify-content: center;
            transition: transform 0.3s ease; /* Smooth transition for hover effect */
            position: relative; /* Ensure the logo container is positioned relative for z-index */
            z-index: 1; /* Ensure the logo appears above the grind lines */
        }

            .logo-container:hover {
                transform: translateX(10px); /* Move logo to the right on hover */
            }
    </style>
</head>
<body>

    <h2>Welcome to KhumaloCraft Emporium</h2>
    <p>Welcome to our platform where you can discover exquisite handcrafted products from artisans worldwide.</p>

    <div class="container">
        <div class="row">
            <div class="col-md-12 logo-container">
                <img src="/image/WhatsApp Image 2024-05-22 at 22.10.00_fbeb7824.jpg" class="img-fluid logo">
            </div>
        </div>
    </div>

    <div class="icon-container">
        <!-- Ensure Font Awesome icons are properly included -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

        <!-- Ensure proper classes for Font Awesome icons -->
        <i class="icon fas fa-tools"></i>
        <i class="icon fas fa-hammer"></i>
        <i class="icon fas fa-screwdriver"></i>
        <i class="icon fas fa-paint-brush"></i>
        <i class="icon fas fa-chair"></i>
        <i class="icon fas fa-table"></i>
    </div>

</body>
</html>



